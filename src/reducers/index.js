import {
    example
} from "./Example";
import {
    amazonDomains
} from "./Domain";
import { 
    combineReducers 
} from "redux";

export default combineReducers({
    example,
    amazonDomains
});