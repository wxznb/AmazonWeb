import {
    example
} from "./Example";
import {
    amazonDomain
} from "./Domain";
import { 
    combineReducers 
} from "redux";

export default combineReducers({
    example,
    amazonDomain
});